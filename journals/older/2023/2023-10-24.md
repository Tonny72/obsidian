- [[installations/des/Natzand/TNZ]]
- [[dev/dev languages/Rust]] Option / Some / None
	- In Rust, `Option` is an enumeration type that represents an optional value. An `Option` is either `Some(T)` and contains a value of type `T`, or `None`, which does not contain a value
