---
tags:
  - Python
  - Parquet
---

- # Howto iterate over een custom class [[dev/Python/Python]]
  ```python
  class Temp:
  def __init__(self):
  self.d = {}
  def __iter__(self):
  for thing in self.d.items():
      yield thing
  ```
  benchmarking #Polars #Parquet to a mapped network drive
	- scan max datetime from 132 files: 10.5s
	- filter data from 132 files (6months): 19s
	- **this is too slow**, need better solution then standard Polars
	- using [[dev/Python/duckdb]]
	  
	  ```python
	  import duckdb
	  con = duckdb.connect('H:/SibHistorian/Data/TEST/file.duckdb')
	  con.sql("CREATE TABLE month AS SELECT * FROM 'H:/SibHistorian/Data/TEST/month/*.parquet';")
	  ```
	  runtime: 6m22s
	  ```python
	  con.sql("select max(datetime) from month")
	  ```
	  ==runtime: 202ms== Blazingly fast