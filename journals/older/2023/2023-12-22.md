- ThreadPool <mark style="background: #BBFABBA6;">for</mark> [[dev/Python/Python]]
	- ```python
	  results = []
	  with ThreadPool(1) as pool:
	    args = [(datastores, source, destinations, tag_name, tag) for tag_name, tag in tags]
	    results = pool.starmap(import_tag, args)
	  ```