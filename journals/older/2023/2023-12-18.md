- playing with [[applications/pkm/LogSeq]] todo's
	- TODO NOW Determine the cost of cheese, per taco.
	        projects:: [[Product Costing]]
	        Effort:: 8
- DONE test
	- duration:: 8
	-
-
- [[applications/pkm/LogSeq]] queries
  
   #+BEGIN_QUERY
    {
    :title [:h2 "Open Tasks <% current page %>"]
    :query [
       :find (pull ?b [*])  ;; get all blocks and put them in variable ?b
       :in $ ?tag
       :where
        [?p :block/name ?tag] ; name is always lowercase
        [?b :block/refs ?p] ; this block references ?p as oppose to being on ?p through :block/page.
       [?b :block/marker ?marker]
       [(contains? #{"TODO" "DOING"} ?marker)]
   ]
   :inputs [:current-page]
   :result-transform (fn [result] (sort-by (fn [r] (get-in r [:block/page :block/journal-day])) result))  ; Sort by the journal date
   :table-view? false
   :breadcrumb-show? false
   :collapsed? false
   }
   #+END_QUERY
- {{query (task TODO)}}
  query-properties:: [:block :page]
  query-table:: true
  query-sort-by:: block
  query-sort-desc:: true
-
-